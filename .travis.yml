language: python

python:
  - "3.4"
  - "3.5"

env:
  - TOXENV=py34
  - TOXENV=py35

install:
  - pip install tox
  
script:
  - tox
  
after_success:
  - pip install coverage codacy-coverage
  - covegare xml
  - python-codacy-coverage -r coverage.xml
